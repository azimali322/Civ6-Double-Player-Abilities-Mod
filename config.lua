-- Configuration for Double Player Abilities Mod
local config = {
    MULTIPLIER = 2, -- Default multiplier (can be changed in game)
    MIN_MULTIPLIER = 1,
    MAX_MULTIPLIER = 10,
    DEBUG_MODE = true -- Enable debug logging for testing
}

return config 